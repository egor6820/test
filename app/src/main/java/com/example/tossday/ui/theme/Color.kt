package com.example.tossday.ui.theme

import androidx.compose.ui.graphics.Color

// Semantic action colors — intentionally distinct from the primary palette
val SwipeGreen = Color(0xFF2ECC71)
val SwipeRed   = Color(0xFFE74C3C)

// Day load indicator colors — semantic, intentionally distinct
val LoadGreen  = Color(0xFF43A047)
val LoadYellow = Color(0xFFFDD835)
val LoadOrange = Color(0xFFFB8C00)
val LoadRed    = Color(0xFFE53935)
